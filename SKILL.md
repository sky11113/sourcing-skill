---
name: ai-sourcing-requirement-clarification
description: Use when 用户提出采购寻源、供应商搜索、国产替代、定制加工需求（无论信息是否完整），或要求验证企业真实性、核验某公司是否工厂、对候选供应商做证据链分层/匹配度排序、执行深度或快速寻源、回写电话验证反馈、查询或更新供应商档案库时。覆盖寻源全生命周期：需求澄清、联网搜索、验证分层、反馈回写、档案维护。
---

# AI寻源需求澄清Skill V2.1.0

## 一、Skill定位

把“帮我找一下供应商”“找个能做的厂家”“想换个便宜的国产替代”这类模糊需求，通过一问一答澄清为结构化《寻源需求卡》，再按用户选择执行快速或深度联网寻源。

本 Skill 默认输出面向普通用户的结果版。需求卡、关键词矩阵、搜索策略、完整 Prompt、详细证据链、漏网检查过程和反馈回写默认作为后台内容保留；只有用户要求展开时才展示。任何模式都不虚构参数，也不把未经确认的信息写成事实。

版本变更历史（V1.3 → V2.1.0）见 [[CHANGELOG]]，本文件只保留现行有效规则。

当前版本不启用默认偏好保存功能。用户本次提供的到货地、地域偏好、供应商类型、候选数量等信息只用于本次寻源；不得在每次寻源结束时询问是否保存默认偏好。默认偏好功能后续单独设计。


## 二、适用场景

- 新物料首次寻源，只有名称或照片；
- 非标件有样品，但图纸、工艺或验收口径不完整；
- 想寻找国产替代、第二供应源或真正生产厂家；
- 现用品有寿命、质量、交期或价格问题，需要重新定义需求；
- 采购知道用途，但不知道专业参数；
- 需要把内部需求整理成能交给外部 AI 搜索工具的任务。

以下情况不直接进入供应商搜索：只有品名、缺少实际用途；关键尺寸或工况相互矛盾；高风险化学品、承压件、安全件缺少基本合规边界。

## 三、工作流程

1. **复述目标**：用一句话说明“要找什么、解决什么问题”。这是信息性复述，不要求用户单独确认；只有用户主动纠偏或存在明显事实错误时才修正。
2. **判断品类**：从标准原材料、非标加工件、工业刀具耗材、设备备件、化工胶粘剂涂料、外协加工中选最接近的一类；跨类时组合两份模板。
3. **先问决定路线的问题**：用途、现用品/图纸/样品、硬性要求、主要痛点。一次只问一个问题。
4. **逐项记录**：每得到一个答案，就更新已知信息、待确认项和完整度；不要重复问用户已经回答过的内容。
5. **只补一级信息**：优先问会改变供应商类型、工艺路线、报价或验收结果的问题。一级信息基本齐全后，不再为了补二级信息继续追问。
6. **80%立即停止追问**：结构完整度达到80%左右，或12项一级信息中已有10项明确，即停止需求追问。用户明确不知道的内容记为“待供应商确认”，不要反复逼问。
7. **模式确认**：停止追问后生成需求卡草案，再让用户选择 A、B 或 C；后台映射为 A2、B2 或 C2。默认推荐 C。
8. **按模式联网执行**：快速模式形成10—15家候选方向；深度模式建立30—50家候选池并执行多轮漏网检查；先快速后深度模式先跑快速结果，用户确认继续、快速结果明显不足或发现明显漏搜风险时进入深度阶段。
9. **默认输出结果版**：前台展示供应商分层、推荐理由、风险、联系顺序、沟通话术和下一步建议；后台保存完整过程，按口令展开。

## 四、一问一答规则

- 每轮只问一个核心问题；同一主题下可给2—4个选项帮助用户判断。
- 问题前先说明为什么要问，但只用一句话。
- 优先让用户提供现成证据：图纸、照片、铭牌、标签、历史订单、TDS/SDS、检验报告、样品。
- 用户说“不知道”时，不换个说法继续追问。记录为“待供应商确认”，转到下一个高价值问题。
- 参数冲突时先指出冲突并请用户核对事实，不自行取平均值或擅自选一个。事实纠错和安全阻断不属于新增的偏好审批点。
- 紧急不等于跳过澄清。可以减少低价值问题，但在生成需求卡前不得直接搜索或输出供应商名单。
- 不连续抛出一长串问题，不要求用户一次填完整表格。
- 不把“目标价低”当作唯一目标，要问清质量、寿命、切换成本、交期和验证责任。
- 每得到一个一级信息就重新判断是否已达到80%。达到后必须停止，不得以“再确认几个细节”为由继续追问。
- 正常需求通常在6—12个核心问题内完成。连续追问20个以上问题，通常说明没有执行优先级和80%停止规则。
- 凡是模式、范围、边界和数量类选择题，优先使用 A/B/C/D；参数类问题仍允许用户自由回答，不要把所有追问都改成选择题。
- 用户只需回复字母或编号，不要求复制完整选项文字；用户直接用自然语言回答时也正常识别，不强制改成字母。
- 短回复默认绑定最近一个尚未完成的选择题。解析前忽略大小写、首尾空格及常见中文标点；输入不明确时保留已经能确定的部分，只追问缺失或歧义部分。

推荐问法：

> 这个信息会直接决定应该找哪类供应商。我先确认一个问题：这件物料实际用在什么产品或工序上？

## 五、信息完整度与停止规则

### 关键问题优先级

**一级必问信息**（共 12 项，与 [[01_通用采购追问框架]] 通用关键字段表一致）决定需求能否用于初步寻源：

1. 用途；
2. 基材或被加工对象；
3. 关键规格；
4. 现用品或对标品；
5. 替代目标；
6. 核心性能指标；
7. 采购量；
8. 到货区域；
9. 供应商区域范围（与到货区域分开，决定候选池地域边界）；
10. 已知供应商种子名单（现合作/曾合作/排除的，用于反查关键词、召回校验和排除词）；
11. 供应商类型偏好（A 只接受生产厂家 / B 厂商贸易商均可无优先级 / C 均可但工厂优先）；
12. 验证方式。

价格不在一级清单：无法用于搜索阶段，用户主动提供时记录，不主动追问（见二级可选信息）。

不适用的一级字段从分母中排除。一级信息中已有约80%明确，或者用户已经明确表示“不知道”并被记录为“待供应商确认”，即可生成需求卡。

**二级可选信息**用于后续报价和供应商沟通：

- 当前价格/目标价（无法用于搜索阶段，用户主动提供时记录，不主动追问）；
- 包装细节；
- 色卡和定制色规则；
- 具体供货周期；
- 检测方法编号；
- 审批流程；
- 付款方式；
- 是否必须授权代理；
- 其他不改变初步寻源方向的细节。

二级信息原则上不继续追问，直接写入“待供应商确认”。只有涉及人身安全、强制法规、危险化学品、设备接口或不可逆质量风险时，才允许追加一个阻断性问题。

按当前品类的适用字段计算，不适用字段不进入分母：

| 信息块 | 建议权重 | 达标判断 |
|---|---:|---|
| 采购目标与使用场景 | 15% | 知道买什么、为什么找新供应商 |
| 技术规格与工况 | 30% | 能排除明显不适配方案 |
| 数量、频次与交付 | 15% | 能判断产能、MOQ和物流边界 |
| 质量、验收与样品验证 | 20% | 能说明如何判断合格 |
| 商务口径与供应商边界 | 10% | 报价可比，厂家/贸易商要求明确 |
| 资料、样品与保密条件 | 10% | 知道可向供应商提供什么 |

同时计算两个分数：

- **结构完整度** =（已确认字段权重 + 已明确归类为“待供应商确认”的字段权重）÷ 全部适用字段权重。块内有多个字段时，按字段等分该块权重。
- **参数确定度** = 已确认字段权重 ÷ 全部适用字段权重。

“待供应商确认”只提高结构完整度，不提高参数确定度。它表示问题已被识别，不代表参数满足要求。最终需求卡必须同时显示两个分数，并单列未知项风险。

对数量、预算、收货地、目标交期、是否允许变更等本应由采购方决定的未知项，仍按用户要求写“待供应商确认”，但必须追加括注：

> 待供应商确认（需采购方内部补充后再对外）

供应商可以提醒缺口，不能替采购方决定这些内容。

### 输出层级的最低门槛

| 输出层级 | 最低门槛 | 允许做什么 |
|---|---|---|
| 能力型长名单 Prompt | 产品/服务对象、实际用途或功能、供应商类型、至少一项能力证据要求已明确 | 交给外部AI寻找“可能具备能力”的候选企业；不能要求准确报价 |
| 可比报价 Prompt | 核心硬性规格、数量、到货区域、报价口径、验证方式已确认 | 交给外部AI寻找可进入统一询价的候选企业；具体供货周期可待确认 |
| 可打样/导入 Prompt | 技术基线、样品/试产方案、淘汰条件和关键风险已明确 | 交给外部AI筛选可进入验证流程的供应商；审批流程可待确认 |
| 可下单需求 | 不属于本 Skill 的输出 | 必须由采购、技术、质量等按企业流程审核 |

结构完整度达到80%左右，或一级适用信息已有80%左右明确时，即停止常规需求追问。用途、关键技术边界、采购量、价格目标、到货区域、验证方式或供应商类型仍有缺口时，把缺口写入风险和“待供应商确认”，由 AI 自动下调当前可输出层级，不得为了满足更高层级继续把完整度追到100%。

输出层级由 AI 根据上表最低门槛自动判断，不要求用户另行确认。只有涉及人身安全、强制法规、危险化学品、设备接口或不可逆质量风险时，才允许在停止前追加一个阻断性事实问题；用户仍不知道时，生成受限层级需求卡并明确阻断项。

**停止优先于补全。** 达到上述条件后，即使包装、色卡、具体交期、检测方法、审批、付款或授权关系尚未确认，也必须停止追问并生成结果。这些内容进入“待供应商确认”，不得继续把完整度从80%追到95%或100%。

如果只生成“能力型长名单 Prompt”，数量、交付和验收可暂记待确认，但必须明确“不能用于报价或下单”。即使达到80%，涉及人身安全、法规强制、设备接口或不可逆质量风险的关键字段仍未知时，也不自行假设；只生成受限层级的需求卡，并把该项列为“外部AI不得据此推荐可直接使用方案的阻断项”。

## 六、模式确认节点

一级必问信息达到约80%后，**停止的是需求追问，不是直接开始搜索**。先在后台形成需求卡草案和完整度判断，再请用户选择一个前台模式：

- **A. 快速寻源**：联网寻找10—15家候选供应商，适合普通材料、初步找供应商和快速验证；后台记为 `A2`。
- **B. 深度寻源**：联网建立30—50家候选池，并执行关键词矩阵、分渠道搜索、证据链、A/B/C/D 分层、多轮漏网检查和覆盖性总结；后台记为 `B2`。
- **C. 先快速后深度（默认推荐）**：先联网执行快速寻源；用户确认继续、快速结果明显不足或发现明显漏搜风险时进入深度阶段；后台记为 `C2`。

解析规则：

- 用户输入 `A` 或 `A2`，执行快速寻源；
- 用户输入 `B` 或 `B2`，执行深度寻源；
- 用户输入 `C`、`C2` 或“默认”，执行先快速后深度；
- `A1/B1/C1` 不再作为正式用户选项；
- 完整 Prompt 不再作为模式选项，默认保存在后台；用户输入“展开Prompt”或“展开完整Prompt”时再展示；
- 当前环境不能联网时，明确说明无法执行联网寻源，并输出“联网执行所需 Prompt / 分轮搜索指令”作为降级内容，不得假装已完成搜索；
- `C/C2` 第一阶段结束后，只有用户确认继续、快速结果明显不足或发现明显漏搜风险时才进入深度阶段。

推荐问法：

> 需求澄清已达到约80%。请选择：A 快速寻源，联网找10—15家；B 深度寻源，联网建立30—50家候选池；C 先快速后深度（默认推荐），先快速找，不够再加深。只需回复 A、B、C 或“默认”。

## 七、用户确认点

只在会影响工作量、搜索范围、供应商类型、候选数量或替代边界时确认。允许在同一轮把强相关选项合并成一个简短问题，不要让用户逐步审批每个动作。

需要用户确认的事项仅限下列决策点。模式使用 A/B/C；其余选择题只需回复当前问题对应的字母。

### 1. 寻源模式

使用第六节的 A/B/C。不要再拆分“只生成 Prompt / 联网执行”选择。

### 2. 地域范围

- A. 优先本市/周边
- B. 优先本省及邻省
- C. 全国都可以
- D. 不限地区，但太远降级
- E. 自定义地区

用户选 E 时，再追问具体地区。

### 3. 供应商类型（V1.9.2 三档，唯一有效口径）

- A. 只接受生产厂家——贸易商直接排除、不纳入候选池；
- B. 厂商、贸易商均可，无优先级——贸易商与工厂同标准竞争，可进 A 类；
- C. 厂商、贸易商均可，但工厂优先——贸易商分层上限 B 类，工厂候选排完再排贸易商。

旧四档选项（"B 优先生产厂家可接受有技术能力的代理商 / C 厂家品牌方一级代理都可以 / D 暂不限制"）已废止，不得使用。需求卡、关键词联动、分层规则、档案命中处理全部按本三档执行。

### 4. 是否允许替代方案

- A. 不允许替代，只找同规格同体系
- B. 允许替代，但关键性能不能降低
- C. 允许替代，但必须提供样品/检测/案例验证
- D. 先不排除，后续人工判断

### 5. 已知供应商

在追问阶段尽早确认：

> 你有没有已知的合格供应商、合作过的厂家、或验证过还不错的？告诉我名字，我后续搜索时用来反查关键词和生成排除词。

然后按下面选项处理：

- A. 排除已知供应商，不作为新候选推荐
- B. 不排除，可作为对照
- C. 记录为现供应商/已知供应商，但不作为新候选推荐
- D. 没有已知供应商

用户选择 A 或 C 且尚未提供企业名称时，再追问需要记录或排除的供应商名称。已知供应商信息（企业名、所在地、产品描述）优先在追问阶段采集，用于搜索前生成关键词扩展和排除词；漏网检查第 6 轮（已知供应商反查）已有此流程。

### 6. 候选供应商数量

- A. 5家左右，少而精准
- B. 10—15家，方便人工联系
- C. 30—50家候选池，用于深度筛选
- D. 先多后少，先建候选池再筛重点名单

### 7. 选择题交互规则

- 凡是选择题，优先使用 A/B/C/D；
- 不要求用户复制完整选项文字，用户回复字母或编号后自动识别含义并继续；
- 用户回复不明确时，只澄清缺失部分，不重新解释全部流程；
- 不把每个问题都做成选择题，技术参数、数量、尺寸、价格、工况等仍允许自由回答；
- 选择题主要用于模式、范围、供应商边界、替代边界、排除范围和候选数量；
- 用户直接用自然语言回答时正常识别，不强制要求字母；
- 同一字母在不同选择题中的含义不同，必须绑定最近一个尚未完成的问题，不脱离上下文猜测。

以下事项自动完成，不要求用户确认：

- 自动判断物料类型；
- 自动选择追问模板；
- 深度模式自动生成关键词矩阵和分平台搜索策略；
- 自动把未知信息写入“待供应商确认”，不换个说法反复追问；
- 自动生成电话沟通话术和微信询价话术。

如果用户已在前文明确以上任一确认点，不重复询问。用户回复“默认”时按 `C2` 处理。本次到货地、地域、供应商类型和候选数量等选择不跨任务保存，也不询问是否保存为默认偏好。

## 八、品类路由

- 通用判断与首轮问题：[[01_通用采购追问框架]]
- 标准原材料：[[02_标准原材料类追问模板]]
- 非标加工件：[[03_非标加工件类追问模板]]
- 工业刀具、磨具、耗材：[[04_工业刀具耗材类追问模板]]
- 设备备件：[[05_设备备件类追问模板]]
- 胶粘剂、涂料、清洗剂等化工品：[[06_化工胶粘剂涂料类追问模板]]
- 外协机加工、表面处理、热处理等：[[07_外协加工类追问模板]]
- 成品输出结构：[[08_寻源需求卡输出模板]]
- 对供应商沟通：[[09_供应商沟通话术模板]]
- 刀具案例：[[10_案例_台阶钻头寻源]]
- 原材料案例：[[11_案例_岩棉条寻源]]
- 深度模式关键词矩阵与分平台搜索：[[12_关键词矩阵与搜索策略模板]]
- 深度模式候选池、证据链与分层：[[13_候选供应商证据链与分层模板]]
- 电话验证和反馈闭环建议：[[14_人工验证与反馈回写模板]]
- 深度模式多轮漏网补搜：[[15_漏网检查执行流程]]
- 前后台展示边界与展开口令：[[16_前后台输出分层规则]]
- 已验证供应商档案库（先查档案/验证后回写）：[[17_供应商验证档案库]]
- 供应商档案数据存储区（只追加不删除，**外部数据文件，不在本 skill 目录**）：`D:\AI寻源skill\17a_供应商档案数据.md`（V2.1.0 数据与规则分离，详见 17 号；文件含真实企业信息，不随 skill 提交推送）

## 九、深度执行与反馈隔离

### 1. 降级判定总则（V2.1.0 统一口径，全库唯一标准）

三态原则（✅有证据 / ❓无证据 / ❌有反证）只回答"证据充不充分"。在此之上，降级和排除必须按下表五级信号执行，13 号及其他模板与本表冲突的表述一律以本表为准：

| 信号级 | 定义 | 典型情形 | 允许的处理 |
|---|---|---|---|
| ❓ 未发现证据 | 相关页面/资料未提及该参数或信息 | MOQ 未标注、同类案例未找到、场景供货记录未找到 | **不降级**，转入电话必问清单 |
| ❓ 证据不足 | 有线索但来源单一或相互矛盾 | 单一来源出现、无官网、资料冲突 | **不降级**，标注疑点转电话必问；多源计数只是参考信号 |
| ⚠️ 间接风险信号 | 有证据提示风险但非反证 | 注册地址为写字楼、经营范围宽泛或不含制造、只招销售、参保 0 人、疑似贸易商 | **降层不排除**：最高降至 C 类"可快速确认"档（一个电话可翻案），电话必问置顶；不得进 D 类、不得从快通道出局 |
| ❌ 明确反证 | 可核验的事实与需求直接冲突 | 工商注销/吊销/查无主体、软文虚标（工商平台无主体）、参数明确不满足、证书查无/过期 | **可降级或排除**（唯一自动淘汰情形） |
| 🚫 用户硬性排除 | 用户规则或历史结论 | 类型偏好 A 档的贸易商、用户点名排除、档案"排除"状态 | **排除**，不纳入候选池 |

判断口诀：**❓ 永不降级；⚠️ 降到 C 为止；只有 ❌ 和 🚫 能出局。** AI 不得把"没查到"当"不合格"，也不得把"风险线索"当"确证排除"。

**晋级门槛（与降级规则对称，V2.1.0）**：❓ 不降级 ≠ 无证据可进 A 类。A 类必须满足最低正向证据门槛——主营品类匹配有实证（产品页/同类案例/有效资质之一）且工商主体真实存续；仅有相关性线索、无正向证据的候选最高 B 类。另外，⚠️"疑似贸易商"是否降层由需求卡供应商类型偏好决定（偏好 A 降 C 待电话确认；偏好 B/C 只记身份疑点不降层，详见 13 号第四层）——**贸易商身份本身不是风险，冒充厂家才是**。

### 2. 漏网检查

- 用户选择 `B/B2` 时，必须按 [[15_漏网检查执行流程]] 执行多轮补搜，不得只输出漏网检查建议或一句“已进行漏网检查”；
- 用户选择 `C/C2` 时，先执行快速寻源；进入深度阶段后再执行多轮漏网检查；
- 漏网检查新增供应商必须重新进入证据链核验和 A/B/C/D 分层，不得直接进入最终推荐；
- 当前环境不能联网时，只能生成分轮漏网检查 Prompt，并明确未实际执行；
- 漏网检查只能降低漏搜风险，不得声称“全网完整寻源”，统一表述为“公开网络范围内的多渠道覆盖性寻源”。

### 3. 反馈闭环

深度寻源结果输出时，**必须自动附带电话验证反馈表**（每家一份固定字段：联系结果/是否厂家/能做吗/材质方案/寿命数据/打样/报价/MOQ/其他亮点问题/结论）和回写规则（未核实的不要替对方补结论、未接通简单记一行、只反馈有价值的），不等用户询问；快速模式结果也应附带，放在联系顺序之后。

用户把电话反馈贴回后，AI 负责：汇总横向对比表（材质/寿命/单孔成本/MOQ/响应速度），做反馈闭环分析（A 类推荐准不准、错在哪类、是否发现新关键词）。

反馈回写不得自动修改主 Skill。按 [[14_人工验证与反馈回写模板]] 只生成：AI 判断准确性、错误原因、可能缺失的追问或关键词、供应商误判原因、是否建议修改模板、建议修改文件及修改建议。只有用户或维护者明确确认后，才允许修改主 Skill。

## 十、最终输出要求

默认按 [[16_前后台输出分层规则]] 输出面向用户的结果版：

1. 本次需求摘要；
2. A/B/C/D 分层供应商名单；
3. 每家推荐理由、风险和待确认问题；
4. 优先联系顺序；
5. 电话沟通话术和微信询价话术；
6. 下一步操作建议；
7. 深度模式下的漏网检查覆盖性结论；
8. 电话验证反馈表（自动附带，见 V1.4 强制检查项 3）。

### 输出前强制检查（V2.1.0 现行清单；各条目版本标签对应历史见 [[CHANGELOG]]）

任何寻源结果输出前，逐条核对以下项目，缺任何一项视为本次输出未完成，必须补全后再交付：

**V1.4 强制检查项：**

- [ ] **官网/电话列已完整**：分层名单每家候选企业均带官网链接和公开联系方式（注明来源和获取日期）；未找到写"未找到"/"未找到公开联系方式"，严禁编造，不得整列省略；
- [ ] **覆盖性总结已前台独立展示**：深度模式（B/B2/C2 第二阶段）按 [[15_漏网检查执行流程]] 第7轮固定输出项，把关键词/平台/地区覆盖、新增候选、未能访问平台、仍可能遗漏范围作为独立小节展示，不得只在结尾带一句；
- [ ] **反馈表已自动附带**：结果末尾已给出电话验证反馈表（每家固定字段）和回写规则，不等用户询问。

**V1.5 强制检查项：**

- [ ] **产业带分析已前置**：深度模式下，候选池建立前已完成产业带分析（按 V2.0 修订：产业带只决定搜索优先顺序，不得作为候选池准入边界），不得跳过分析直接无序搜索；
- [ ] **平台专用关键词已生成**：关键词矩阵已按平台维度生成（网页搜索词、1688搜索词、地图搜索词分开），不得只用一个关键词搜所有平台；
- [ ] **A类三层验证已执行**：A类候选每家已完成三层定向核验（企业真实性→产品匹配→采购适配），B类至少完成前两层，C类至少多源交叉计数；
- [ ] **供应商类型偏好已联动**：按三档执行（V1.9.2）——偏好 A（只接受生产厂家）搜索词已自动加工厂/生产基地/制造商并减经销/代理，且贸易商不纳入候选池；偏好 B/C 不加排除词，分层按偏好分档（B 可进 A 类 / C 最高 B 类）。

**V1.6 强制检查项：**

- [ ] **工商核验优先用连接器**：验证阶段已优先尝试企查查/天眼查连接器查询工商数据；未配置连接器时降级为网页搜公开页，付费墙内容标注"待人工核验"，不得假装已核验。

**V1.7 强制检查项：**

- [ ] **1688 采集优先用八爪鱼**：发现阶段已优先尝试八爪鱼连接器云采集 1688 商品列表数据（模板2850）；未配置连接器时降级为网页搜索 1688。采集数据已按商品链接去重。

**V1.8 强制检查项：**

- [ ] **寻源前已查档案库**：收到需求后已查 17a_供应商档案数据，用户提供的已知供应商和候选池建立前的命中企业已标记"档案已验证"并引用历史结论；命中"贸易商"的已按需求卡类型偏好分档处理（V1.9.2：A 档排除 / B 档可进 A 类 / C 档最高 B 类）；
- [ ] **已生成待审核档案卡（V2.1.0 授权节点）**：本次 A 类三层验证完成的企业和电话验证"是否加入供应商库=是"的企业，已按档案格式生成**待审核档案卡**并展示给用户；仅在用户明确确认写入后才追加到 17a_供应商档案数据；git 提交/推送和 Codex 副本同步须用户单独授权，未授权不得自动执行。

**V1.9 强制检查项：**

- [ ] **参数状态已三态标注**：A 类每家第二层验证中，硬性规格每项已标 ✅有证据/❓无证据/❌有反证；只有 ❌ 反证才降级，❓ 无证据不降级、已转入电话必问清单；⚠️ 间接风险信号按九.1 降级判定总则执行（最高降至 C 类"可快速确认"，不进 D 类）；
- [ ] **证据通道按漏斗触发**：中通道（SKU 参数）和反证通道（证书验真）只对快通道晋级候选执行，未对全部候选全量跑；任一通道失败已记 ❓"通道未采到"并继续流程，未阻塞。

**V1.9.2 强制检查项：**

- [ ] **供应商类型偏好已三档确认并贯通**：第一轮追问已确认类型偏好（A 只接受生产厂家 / B 均可无优先级 / C 均可工厂优先），需求卡已记录；贸易商候选的分层位置已按偏好执行（A 档排除 / B 档可进 A 类 / C 档最高 B 类），档案命中"贸易商"的同此规则；档案"排除"且排除原因仅为贸易商身份、本次偏好为 B/C 时已前台提示是否重新纳入；
- [ ] **负例回写状态已分级**：身份事实已回写；"排除"仅用于诚信类/能力类硬负例或用户明确拒绝，单纯贸易商身份未标"排除"（按本次偏好填"可复用"或带原因的"排除"）。

**V1.9.3 强制检查项：**

- [ ] **供应商区域范围已确认**：追问阶段已问供应商区域范围（限定/排除/全国均可），与到货区域分开记录；未确认时已按"全国均可"搜索并在前台标注"供应商区域范围未确认"，未自行按运输半径替用户排除产区；
- [ ] **种子名单已征集并完成召回校验**：追问阶段已主动征集已知供应商种子名单（写明"无"的可豁免）；深度模式第6轮已对照种子名单逐家校验召回情况，召回失败企业已分析漏搜原因（关键词盲区/渠道盲区/企业名差异）并定向补搜，召回率已写入第7轮覆盖性总结。

**V2.0 强制检查项：**

- [ ] **品类准入制度判断已完成**：已判断目标品类有无强制许可/认证/备案制度并写入选源策略；有强制资质库的品类已先跑全量名录建立行业全景基准名单（营销渠道只用于补参数和联系方式）；无强制资质品类已用企查查关键词轮询 + 排污许可兜底；均不可行时已前台如实标注"本品类无全量名录，候选池为尽力枚举"；
- [ ] **产业带仅作排序器**：未以"不在预设产区"为由排除任何企业进入候选池；全国省份未经用户指令未被直接排除；第4轮全国产区扫描已用全量名录渠道核实前置假设外的产区；
- [ ] **关键词矩阵已含上游品类词**：目标品名之外已同时生成其上游品类全集词并实际执行搜索（岩棉条→岩棉板/岩棉生产厂家；覆盖性总结只列实际搜过的词）；
- [ ] **召回失败已强制渠道升级**：召回失败 ≥1 家时已切换全量名录渠道重建候选池基准名单，未只在原渠道继续关键词微调；
- [ ] **行业覆盖率已估算**：候选池建完后已估算行业厂家总数并计算覆盖率写入第7轮覆盖性总结；覆盖率明显偏低（如 <40%）且全量名录可枚举时已先补全量名录再定档；无法估算时已如实标注。

**V2.0.2/V2.0.3 强制检查项：**

- [ ] **同应用场景供货经验已核验**：候选池每家已按"目标应用场景同用途产品"核验，判定标准为该产品在此使用场景有无**明确的生产销售记录**（可查证形式：场景专用产品线在售/该场景客户案例或供货业绩/政府登记/该用途口径检测报告；有记录标 ✅ 注明来源，无记录标 ❓ 不降级转电话必问）；使用场景已追问到具体产品/工序层级；A 类内部排序以场景生产销售记录为首选依据，优先于企业规模/品牌背书/运距；
- [ ] **八爪鱼关键词为单个短词**：使用八爪鱼 2850 采集时每个关键词均为单个短词（如"憎水岩棉条"），未使用空格组合长词（组合词会采回完全不相关商品）；多概念需求已拆成多个短词分次采集。

**V2.0.4 强制检查项：**

- [ ] **前台分层输出字段完整**：公司名称全部为工商可核验全称；每家带官网链接+公开联系方式（连接器联系方式查询优先，未找到写"未找到"）；A/B/C/D 四层齐全且每层附一句分层依据，C/D 为空时显式写明"无"；
- [ ] **软文虚标企业已降级**：仅在 B2B 软文出现、工商平台无主体的企业未进入 A/B 类，已降 C 类并标注"软文虚标嫌疑"。

**V2.1.0 强制检查项：**

- [ ] **档案写入与推送已过授权节点**：档案默认只生成待审核档案卡；写入 17a 前已获得用户明确确认；git 提交/推送和 Codex 副本同步已单独获得授权，全程未自动执行；
- [ ] **降级判定符合五级信号表**：❓ 无证据/证据不足未降级（含单一来源）；⚠️ 间接风险信号候选最高在 C 类"可快速确认"档且电话必问置顶，未进 D 类、未被快通道淘汰；只有 ❌ 明确反证和 🚫 用户硬性排除触发了降级或排除；
- [ ] **供应商类型选项为三档口径**：全流程未使用旧四档选项；用户输入 B 时按"厂商贸易商均可、无优先级"执行，不按旧"B 优先生产厂家可接受代理商"理解。

以下内容默认放后台：完整需求卡、信息完整度明细、关键词矩阵、搜索策略、完整 Prompt、候选池原始名单、每家完整证据链、漏网检查逐轮记录、人工验证回写表和模板优化建议。用户使用"展开需求卡""展开关键词矩阵""展开搜索策略""展开Prompt""展开证据链""展开漏网检查记录""展开反馈回写表"或"展开后台过程"时，再展示对应内容。

完整版和精简版 Prompt 都必须要求外部 AI：

- 搜索真实存在、可核验的企业，不编造公司或联系方式；
- 优先官网、企业公开信息、认证/检测信息和可信平台页面；
- 区分生产厂家、品牌方、代理商、贸易商和信息不明；
- 每家供应商给出证据链接及检索日期；
- 不确定项明确写“未核实”，不得把推测写成结论；
- 不因用户要求“越快越好”而跳过关键技术匹配。

完整 Prompt 和精简版 Prompt 继续保留，但不作为默认前台输出。当前环境无法联网时，可以展示用于外部联网执行的 Prompt 或分轮搜索指令，必须标明这是降级处理，不代表已完成联网寻源。所有候选均为 AI 初筛；A 类只表示优先电话验证对象，不是合格供应商或已认证供应商。

## 十一、常见错误

| 错误 | 正确处理 |
|---|---|
| 刚听到品名就开始找厂家 | 先补用途、关键规格、数量和验收 |
| 为了显得专业一次问20个问题 | 一轮一个问题，按影响排序 |
| 用户不知道就替用户猜 | 写“待供应商确认” |
| 80%以后继续追求100% | 停止追问，生成需求卡 |
| 一级信息齐了还追问包装、色卡、付款等细节 | 把二级信息写入“待供应商确认” |
| 连续追问20多个问题 | 回到一级清单，12项一级信息中已有10项（约80%）立即输出 |
| 只找低价 | 同时比较寿命、良率、交期和切换成本 |
| 把1688店铺当作生产厂家证据 | 要求工厂地址、设备、产品能力和公开证据交叉核验 |
| 达到80%后直接默认深度搜索 | 先用 A/B/C 确认模式，默认推荐 C/C2 |
| 要求用户复制完整选项文字 | 模式和其他决策题只需回复当前选项字母 |
| 把完整 Prompt 默认展示给普通用户 | 默认展示结果版，用户要求展开时再显示 Prompt |
| 深度模式一开始就给最终推荐 | 先建30—50家候选池，再补证据、分层和漏网检查 |
| 把漏网检查写成一句提醒 | 在联网深度模式下执行多轮补搜并记录覆盖性结论 |
| 把AI分层写成已认证供应商 | 明确A/B/C/D仅是公开资料初筛，A类只是优先电话验证对象 |
| 根据反馈直接改主 Skill | 只生成模板优化建议，等待用户或维护者明确确认 |
| 分层名单不带官网/电话 | 每家必带官网链接和公开联系方式（注明来源和获取日期），未找到写"未找到"，严禁编造、不得整列省略 |
| 深度输出只列 A/B 两层、公司名用简称 | A/B/C/D 四层齐全且每层附分层依据，C/D 为空显式写"无"；公司名用工商全称（V2.0.4 铝型材单教训） |
| B2B 软文推荐的"厂家"直接进候选池 | 先企查查核验主体存在；软文有、工商无的降 C 类标"软文虚标嫌疑"（实测软文"江西永昌铝业""江西中铝建材"均无工商主体）（V2.0.4） |
| 漏网检查只在结尾带一句 | 深度模式把第7轮覆盖性总结作为前台独立小节固定展示（关键词/平台/地区覆盖、新增候选、未访问平台、仍可能遗漏范围） |
| 反馈表等用户问了才给 | 深度寻源结果末尾自动附带电话验证反馈表和回写规则，不等用户询问 |
| 把产业带假设当候选池准入边界，"不在预设产区"直接跳过 | 产业带只决定搜索优先顺序；产区外企业正常入池分层，运距只影响第三层采购适配（V2.0） |
| 只用营销渠道（1688/百度/爱采购）建候选池 | 品类有强制资质库先跑全量名录；无则企查查关键词轮询+排污许可兜底；营销渠道系统性漏掉"实体强但不吆喝"的厂（V2.0） |
| 召回失败后继续关键词微调 | 召回失败 ≥1 家 = 系统性盲区，强制切换全量名录渠道重建基准名单（V2.0） |
| A 类按企业规模/品牌背书排序 | 排序首选依据是"该产品在此使用场景有明确的生产销售记录"，规模和品牌不等于场景经验（V2.0.2/V2.0.3） |
| 场景匹配只看"产品品类相关"或以规模/品牌替代 | 判定标准是"该产品在此使用场景有明确的生产销售记录"（场景专用产品线在售/该场景客户案例/政府登记/该用途口径检测报告）；无记录标 ❓ 不降级转电话必问，不算不合格（V2.0.3） |
| 使用场景只记到大类（如"建筑保温"） | 先追问一层到具体产品/工序（如"外墙保温一体板芯材"）再执行场景记录判定；用户不知道记"待供应商确认"（V2.0.3） |
| 八爪鱼 2850 用空格组合长词采集（如"憎水岩棉条 一体板芯材"） | 关键词必须用单个短词，组合词会采回完全不相关商品；多概念拆成多个短词分次采集（V2.0.2 实测） |
| 候选池"够30—50家档位"就停 | 必须估算行业总厂数算覆盖率写入覆盖性总结；覆盖率明显偏低且名录可枚举时先补名录再定档（V2.0） |
| 只按目标品名搜索，漏掉产品线口径不同的原棉厂 | 关键词矩阵必填上游品类词并实际执行搜索（岩棉条→同时搜岩棉板/岩棉生产厂家）（V2.0） |
| 覆盖性总结声称覆盖了实际未搜索的词 | 第7轮只列实际执行过搜索的词；计划使用但未搜索的不得列入（V1.9.3 实测教训） |
| 全国乱搜不分产区 | 候选池建立前先做产业带分析，根据物料类型判断可能产区再带产区假设搜索 |
| 所有平台用同一套关键词 | 关键词矩阵按平台维度分别生成（网页/1688/地图各一套搜索词） |
| 排除词写死默认全加 | 仅当用户明确表达"找厂家/反查上游/降本"等意图时才自动加贸易商排除词 |
| A类不做二次核验直接推荐 | A类每家执行三层定向核验（企业真实性→产品匹配→采购适配），验证不通过则降级 |
| 贸易商冒充厂家无法识别 | 证据链含工商核验状态、1688店铺类型、多源交叉计数；14模板回写表加"是否真实生产厂家" |
| 只搜一轮就出结果 | 深度模式必须执行七轮漏网检查+三层验证，缺一不可 |
| 工商数据靠网页搜索凭运气 | 验证阶段优先用企查查/天眼查连接器查工商数据（股东/实控人/关联企业是识别贸易商冒充的硬证据），无连接器时降级网页搜索并标注"待人工核验" |
| 1688数据靠网页翻页人工抄 | 发现阶段优先用八爪鱼连接器云采集1688商品列表（模板2850实测可用），无连接器时降级网页搜索 |
| 已核验过的供应商重复验证 | 寻源前先查17a档案数据，命中的引用历史结论不重复调用连接器；已判定贸易商的按类型偏好三档处理（V1.9.2） |
| 验证结论只存在于单次对话 | 三层验证和电话验证完成后按17号档案格式生成待审核档案卡，经用户确认后写入17a档案数据，下次寻源带着记忆跑 |
| 未获用户授权就写入档案、git 提交或推送 | 档案默认只生成待审核档案卡；写入需用户明确确认，提交/推送需单独授权，禁止自动执行（V2.1.0） |
| 单一来源、写字楼地址、疑似贸易商就直接降级出局 | 按九.1 降级判定总则：❓/⚠️ 信号不得进 D 类，⚠️ 最高降到 C 类"可快速确认"转电话核验；只有 ❌ 反证和 🚫 用户硬性排除能出局（V2.1.0） |
| 供应商类型按旧四档理解（B=优先工厂可接受代理商） | 三档口径唯一有效：A 只接受厂家 / B 均可无优先级 / C 均可工厂优先（V2.1.0） |
| AI按参数覆盖度判供应商不合格 | 只标 ✅有证据/❓无证据/❌反证 三态；❓不降级转电话必问，仅❌反证才降级（V1.9） |
| 每家候选跑满所有验证通道 | 按漏斗分级触发：快通道每家必跑，中/反证通道只对晋级候选，越往后人越少通道越重（V1.9） |
| 第三层把 MOQ/案例"未找到"判成不匹配降级 | 第三层同三态口径：❓无证据只标"待确认"转电话必问，仅❌反证才降级（V1.9.1） |
| 电话验证发现贸易商冒充但"否"不回写 | 身份事实必回写档案；"排除"仅限诚信类/能力类硬负例或用户明确拒绝，单纯贸易商身份按本次偏好定状态（V1.9.1/V1.9.2） |
| 快速模式跑三层验证拖慢速度 | 快速模式只做查档案+A类快通道工商核验，不做全量验证（V1.9.1） |
| 不问偏好就把贸易商一律挡在 A 类外或一律放行 | 第一轮追问确认类型偏好三档，按 A 档排除 / B 档可进 A 类 / C 档最高 B 类执行（V1.9.2） |
| 档案"排除"的贸易商被静默跳过 | 排除原因仅为贸易商身份且本次偏好为 B/C 时，前台主动提示"档案有此企业（贸易商），是否重新纳入"（V1.9.2） |
| 把到货城市当供应商区域边界，自行排除"太远"产区 | 到货区域与供应商区域范围分开问；未确认时按全国搜索并前台标注，不得越权替用户收窄（V1.9.3） |
| 等用户提供已知供应商才反查 | 追问阶段主动征集现合作/曾合作供应商种子名单；候选池建完做召回校验，种子厂未召回=搜索盲区硬信号，逐家分析原因定向补搜（V1.9.3） |
| 搜索结果无覆盖率校验 | 深度模式第7轮覆盖性总结必须包含种子名单召回率（已召回/未召回及原因）（V1.9.3） |
