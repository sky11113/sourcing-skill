# CHANGELOG 寻源 Skill 版本变更史

> V2.1.0 起版本变更史从 SKILL.md 主文件剥离至此，主文件只保留现行有效规则。
> 各版本对应的强制检查项在 SKILL.md「输出前强制检查」中仍保留版本标签，可在此溯源。

## V2.1.0（2026-08-27）

由 Codex 两轮静态评审 + WorkBuddy 两轮实跑（快速+深度）复盘共同驱动，修复规则冲突和安全边界缺陷：

1. **档案写入/推送改为独立授权节点**：AI 默认只生成"待审核档案卡"，用户明确说"写入档案"（或等效确认）后才追加到 17a；git 提交/推送和 Codex 副本同步须用户单独授权，禁止自动执行（废止 V1.9.1"回写后须 git 提交推送+同步 Codex 副本"的自动同步规则）；
2. **统一降级判定总则（五级信号表）**：解决主文件三态原则与 13 号分层规则多处冲突——❓未发现证据/证据不足不降级；⚠️间接风险信号（写字楼地址、经营范围宽泛、只招销售、单一来源等）最高降至 C 类"可快速确认"，不得进 D 类；仅 ❌ 明确反证和 🚫 用户硬性排除可自动降级或排除；多源交叉计数的"单一来源降级"规则废止，改为标注信号不降级；
3. **统一供应商类型选项为三档**（V1.9.2 口径）：A 只接受生产厂家 / B 厂商贸易商均可无优先级 / C 均可但工厂优先；废止第七节遗留的旧四档选项（B"优先生产厂家可接受代理商"/C"厂家品牌方一级代理都可以"/D"暂不限制"）；
4. **统一一级字段为 12 项**：新增"供应商区域范围"（V1.9.3 已实际使用但未入清单）；价格维持二级（用户主动提供时记录）；01 号删除重复的"供应商类型"行；停止口径改为"12 项一级信息中已有 10 项明确"；
5. SKILL.md 版本史剥离至本文件；README.md 同步精简；
6. **17a 数据与规则分离**：17a_供应商档案数据.md（含真实企业信息与核验判断）移出 skill 目录至 `D:\AI寻源skill\`，不随 skill 安装、提交或推送；全库 17a 的 wiki 链接改为纯文本引用，路径在 SKILL.md 文件清单和 17 号第二节标注；文件不存在视为空档案库从零积累；
7. **A 类正向证据门槛**：❓不降级 ≠ 无证据可进 A 类——A 类须有品类匹配实证（产品页/案例/资质之一）+ 工商主体存续；仅相关性线索无正向证据的最高 B 类；
8. **贸易商身份与分层影响分离**：疑似贸易商是否降层由供应商类型偏好决定（偏好 A 降 C 待电话确认；偏好 B/C 只记身份疑点不降层）；贸易商身份本身不是风险，冒充厂家才是；
9. **待实体解析状态**：只有营销名称、无法解析出工商主体的线索不进 A/B/C/D、不计入候选池数量，单独列"待实体解析"清单；
10. **电话证据优先级分领域**：经营事实（MOQ/模具费/接单意愿）以电话为准，法律事实（注册资本/股东/状态）以工商登记为准，冲突标"证据冲突"待人工裁决（取代"电话结论一律最高优先"）；
11. **触发描述扩展**：frontmatter description 覆盖澄清、搜索、验证、匹配、分层、反馈、档案维护全生命周期；
12. **分品类模板口径对齐**：06 号化工模板一级必问与 12 项对齐，价格降二级；01 号"待供应商确认/待采购方确认"按责任归属如实选用，废止固定措辞补丁。

## V2.0.4（2026-08-26）

由铝型材单深度模式执行复盘驱动，修复输出字段完整性执行遗漏（模板 13 号第七节本已规定，属执行不合规而非模板缺陷，故以检查项强化防再犯）：

- **前台分层输出字段完整性**：公司名称必须用工商可核验全称；每家必带官网链接+公开联系方式（企查查/天眼查连接器联系方式查询工具优先，无连接器降级网页搜，注明来源和获取日期，未找到写"未找到"）；A/B/C/D 四层齐全且每层附一句分层依据，C/D 为空时也显式写明"无"，不得只输出 A/B 两层；
- **软文虚标企业降级规则**：仅在 B2B 软文出现、工商平台查不到主体的"企业"（实测：软文"江西永昌铝业""江西中铝建材"在企查查均无主体），不得进入 A/B 类，降 C 类"大概率不相关"并标注"软文虚标嫌疑"（同步写入 13 号第七节输出前检查）。

## V2.0.3（2026-08-26）

由同一实战复盘的追加反馈驱动，把"场景经验"的判定标准收紧为可查证的**明确生产销售记录**：

- **场景匹配判定标准（最准确口径）**：候选对目标使用场景的匹配，最准确的判定标准是"该产品在此使用场景有**明确的生产销售记录**"——可查证记录形式：①该场景专用产品线在售（如"夹芯板芯材条/带"专列型号）；②销往该场景客户的案例/供货业绩；③政府/行业登记（按该用途口径）；④按该用途出具的检测报告。泛泛"产品相关"、企业规模、品牌背书、运距均不能替代场景记录（同步写入 01 号使用场景联动规则、08 号需求卡与 Prompt、13 号字段定义和 A 类排序规则）；
- **追问深度联动**：使用场景必须追到具体产品/工序层级（"建筑保温"→"外墙保温一体板芯材"）才能执行该标准；只答到大类时先追一层，用户不知道记"待供应商确认"并按大类弱匹配标注；
- **三态口径不变**：有明确记录 ✅（注明记录来源）；只有品类相关、无场景记录 ❓ 不降级、排序靠后，转电话必问"有没有给该场景客户供货的记录"。

## V2.0.2（2026-08-26）

由实战复盘"效果不达预期"反馈驱动（用户指出排序偏离），修复候选排序主维度问题：

- **同应用场景供货经验优先（A 类排序首选依据）**：候选池每家核验"目标应用场景同用途产品生产/供货实证"（场景专用产品线/政府登记/同行客户案例/按该用途口径的检测报告），有实证 ✅ 排前，无实证 ❓ 不降级但排序靠后转电话必问——优先级高于企业规模、品牌背书和运距。实战教训：按规模/品牌把品类巨头排在前面，但巨头主业是"岩棉板/保温系统"，用户真实合作厂全部是"夹芯板/一体板芯材条/带"专业厂——规模和品牌不等于场景经验；
- **八爪鱼 2850 短词规则（实测）**：连接器可用（此前"未连接"为误判）；搜索关键词必须用单个短词，空格组合长词会采回完全不相关商品（实测"憎水岩棉条 一体板芯材"组合词采回 10 行全是 T 恤服装）；采集结果为品类模糊匹配（相关但非精确），适合当发现渠道不适合精确筛选。

## V2.0（2026-08-25/26）

由同一实战的**第二轮召回失败**驱动（用户两次报出共 5 家现合作厂，其中 2 家第一轮召回后仍未找到），搜索架构结构性重修：

- **产业带降级为排序器**：产业带假设只决定搜索优先顺序，不得作为候选池准入边界；15 号删除"不覆盖产区"字段改"待核实产区"，第 4 轮改为全国产区扫描（用全量渠道核实，不用同一假设补同一假设的漏）；13 号明确"不得以不在预设产区为由排除企业"；
- **品类准入制度判断 → 全量名录分层**：AI 先判断品类有无强制许可/认证/备案制度——有则官方资质库（SC 证/CCC/绿色建材目录/排污许可等，按品类映射不绑定行业）做全量枚举；无则企查查国标行业枚举 + 排污许可平台兜底；两者皆无（通用加工/贸易品/新兴品类）如实标注"本品类无全量名录，候选池为尽力枚举"。核心认知：营销可见性渠道（1688/百度/爱采购/抖音）系统性漏掉"实体强但不吆喝"的厂（实测：参保 64/77 人的两家原棉厂在 35 家候选池全部缺席）；
- **召回失败强制渠道升级**：召回失败 ≥1 家 = 营销渠道系统性盲区 → 强制切换全量名录渠道重建候选池基准名单，不得继续关键词微调；
- **候选池数量级覆盖率检查**：建池"够档位就停"前必须估算行业总厂数、计算覆盖率写入覆盖性总结（实测教训：35 家 ÷ 行业约 150 家 ≈ 23%，盲区一目了然）；
- **上游品类词规则**：关键词矩阵必填上游品类全集词（岩棉条 → 同时搜岩棉板/岩棉生产厂家）——按目标品名搜索只能搜到"以该品名为卖点"的厂，产品线口径不同的原棉厂（板为主、条能做）只有上游品类词能覆盖。

## V1.9.3（2026-08-25）

由首次实战（憎水岩棉条寻源）反馈驱动，修复两个漏斗入口缺口：

- **供应商区域范围前置确认**：到货区域（物流终点）与供应商区域范围（候选池地域边界）是两个字段，追问阶段分开问；用户未确认时按"全国均可"宽口径搜索并前台标注，AI 不得自行按运输半径替用户排除产区（实战教训：AI 自行排除西北/东北属越权决策）；
- **已知供应商种子名单主动征集 + 召回校验**：追问阶段主动问"现在或以前跟哪些供应商合作过"；候选池建完后对照种子名单做召回校验——种子供应商未出现 = 搜索盲区硬信号，逐家分析漏搜原因（关键词盲区/渠道盲区/企业名差异）并定向补搜，召回率写入漏网检查第7轮覆盖性总结。实战教训：框架原只写"用户提供时执行"，AI 从未主动要名单，导致现合作供应商未被搜出且无校验机制发现。

## V1.9.2

新增**供应商类型偏好三档**，把"贸易商怎么办"从事后补救规则改为前置需求输入，并修正负例回写状态一刀切问题：

- **类型偏好三档（第一轮追问即确认）**：A 只接受生产厂家——贸易商直接排除、不纳入候选池；B 厂商贸易商均可、无优先级——贸易商与工厂同标准竞争，可进 A 类；C 均可但工厂优先——贸易商分层上限 B 类，工厂排完再排贸易商。需求卡、关键词联动、分层规则、档案命中处理全部按此三档执行；
- **"贸易商不得进 A 类"一刀切规则废止**：原写在 08/13/15/17/SKILL 检查项⑩的硬规则全部改为"按需求卡类型偏好分档"；
- **负例回写状态分级（档案存事实，不存武断判决）**：身份事实必回写；"排除"收窄为诚信类（主动冒充/伪造证书/假地址）、能力类（明确不能做/无法满足硬性规格）或用户明确拒绝；单纯贸易商身份 ≠ 排除，按本次偏好决定状态；
- **历史排除记录的重新纳入提示**：档案"排除"且排除原因仅为贸易商身份的企业，本次偏好为 B/C 时前台主动提示是否重新纳入，不静默跳过。

## V1.9.1

全链路审查后的缺陷修复版，堵住三态原则的执行漏洞并补齐边界定义：

- **第三层采购适配同步三态口径**：MOQ/案例等 ❓ 无证据只标"待确认"不降级，仅 ❌ 反证才降级；规格判断复用第二层结果，第三层不重复判合格；
- **快速模式（A2）最小验证集**：只做查档案（必做）+ A 类快通道工商核验；不做三层全量、SKU 核对、证书验真，不生成电话必问清单；
- **负例回写**：电话验证发现重大反证（贸易商冒充等）的企业，即使"加入供应商库=否"也回写档案并标"排除"，防止重复踩坑（V1.9.2 起状态分级）；
- **档案双副本同步**：回写后须 git 提交推送 + 同步 Codex 副本（**V2.1.0 废止，改为独立授权节点**）；
- **反证通道实测结论**：认监委页面可访问但查询需图形验证码，AI 无法自动完成——AI 生成"待验真证书清单"（企业名+证书编号+入口链接）交用户人工查验，查无此证/过期/范围不覆盖记 ❌ 反证；
- **前台展示补齐**：A 类硬性规格三态标注进前台；跨候选聚合"电话必问清单"独立小节展示（深度模式）；B 类第二层明确为浅验证（品类覆盖确认，不逐参数核对）。

## V1.9

新增**参数三态标注**和**证据通道漏斗触发**，解决"AI 按覆盖度误杀合格供应商"和"每家全跑所有验证导致流程又慢又卡"两个缺陷：

- **档案数据独立成 17a**：17 号只保留规则和格式定义，档案记录统一存于 17a，只追加不删除，避免规则文件被数据污染；
- **参数三级分类 + 三态标注**：硬性规格（需求卡第 5 项）/软性规格（第 6 项）/未知规格（待供应商确认）。AI 对每个硬性规格只输出 ✅有证据 / ❓无证据（不降级，转电话必问）/ ❌有反证（唯一允许降级的情形）。核心原则：**AI 判断的只能是证据充不充分，不能是产品合不合格**；
- **证据通道漏斗触发**：通道分快（企查查+店铺页，每家必跑）/中（AI 网页打开 1688 商品详情页读 SKU 参数表，已实测不登录可读约 60/65 规格；晋级后 5-8 家）/反证（认监委 cx.cnca.cn 证书验真，挂证候选）/兜底（电话+寄样，拍板前 1-3 家）。证据投入与候选晋级成正比，淘汰只依据硬规则（反证/注销/用户排除/档案排除），不依据 AI 主观覆盖度判断；任一通道失败记 ❓ 不阻塞流程。专利和标准制定通道不采用（中小供应商覆盖率过低）；八爪鱼 2857 商品详情模板已实测不可用（0 行），不要再选用。

## V1.8

新增**已验证供应商档案库**，解决"历史验证结论零沉淀"的缺陷：

- 新增 17 号，沉淀已验证供应商的核验结论（企业真实性/产品匹配/采购适配/供应商类型判定）；
- 执行三段式流程：**先查档案 → 再新建验证 → 验证后回写**。寻源开始和候选池建立前先查档案，命中的引用历史结论不重复调用连接器；已判定贸易商的企业按类型偏好三档处理（V1.9.2 起修正）；
- 14 号反馈回写落点指向档案库：电话验证"是否加入供应商库=是"的企业按档案格式回写；
- 每次寻源"带着记忆跑"，已核验结论跨任务复用，连接器预算和网页搜索不再重复消耗。

## V1.7

新增**1688 云采集连接器优先**：

- 发现阶段优先通过八爪鱼连接器云采集 1688 商品列表数据（实测可用模板 2850，字段含商品标题/价格/成交量/店铺名称/店铺年限/店铺标签等），以结构化数据批量建候选池；
- 未配置连接器时降级为网页搜索 1688，不阻塞现有流程；
- 采集数据注意两点：跨页重复商品需去重；搜索推荐噪音大时改用更精确的平台专用关键词。

## V1.6

新增**工商核验连接器优先**：

- 验证阶段优先通过企查查/天眼查连接器查询工商数据（存续状态、经营范围、股东结构、实控人、受益所有人、对外投资、关联企业）；
- 未配置连接器时降级为网页搜索公开页，付费墙内容标注"待人工核验"，不得假装已核验；
- 连接器查询给"识别贸易商冒充工厂"提供硬证据：查候选企业股东/关联企业里是否存在真实生产厂，存在→冒充嫌疑降低，不存在→疑似纯贸易商。

## V1.5

进行四个模块的升级，核心目标是**建立"发现供应商 → 验证供应商"闭环**：

1. **多渠道供应商发现**：搜索渠道分为"发现渠道"（1688/百度/地图/爱采购/抖音/行业垂直站）和"验证渠道"（企查查/天眼查/招投标/专利/招聘），产业带分析从漏网检查第4轮前置到候选池建立前（V2.0 修订：产业带只排序不过滤）。六类物料模板（02-07）各配置行业垂直渠道；
2. **平台专用关键词生成**：关键词矩阵增加平台维度，同一物料为网页搜索、1688搜索、地图搜索分别生成不同的搜索词组合。排除词改为条件触发：仅当用户明确表达"找厂家/反查上游/降本"等意图时才自动加贸易商排除词；
3. **三层供应商验证**：候选池建立后，对 A 类每家执行三层定向核验——第一层企业真实性、第二层产品匹配、第三层采购适配。B 类只验第一二层，C 类只做多源交叉计数。证据链新增工商核验状态、1688店铺类型、多源交叉计数字段；
4. **首轮问题升级**：新增一级必问"供应商类型偏好"（联动关键词切换）；新增"图片+技术资料可选上传入口"。

## V1.4

新增三条**输出前强制检查项**，任何一次寻源结果输出前必须逐条核对，缺项即为未完成：

1. **官网/电话必填**：分层名单中每家候选企业必须同时输出官网链接和公开联系方式（注明来源和获取日期）；未找到写"未找到"，严禁编造，不得省略该列；
2. **覆盖性总结前台独立展示**：深度模式下，漏网检查第7轮覆盖性总结必须作为前台固定小节展示，不得只在结尾带一句；
3. **反馈表自动附带**：深度寻源结果末尾必须自动附带电话验证反馈表和回写规则，不等用户追问。

## V1.3

新增"排除/边界词"降噪、已知供应商追问前置反查、证据链字段拆分（匹配证据 + 不匹配风险）、C 类子标注、官网链接和公开联系方式输出，以及价格信息从一级必问降为二级可选。
