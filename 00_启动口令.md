# 00 AI寻源需求澄清Skill V2.1.0 启动口令

以下口令可直接复制使用。无论选择哪种模式，都先自动判断物料类型，一次只追问一个核心问题，并在信息完整度达到约80%后停止需求追问。

达到约80%后，只显示三个前台模式，用户回复一个字母即可：

- `A`：快速寻源，联网寻找10—15家候选；后台等同于 `A2`；
- `B`：深度寻源，联网建立30—50家候选池并执行多轮漏网检查；后台等同于 `B2`；
- `C`：先快速后深度，默认推荐；后台等同于 `C2`。

用户输入 A2/B2/C2 时按原含义执行；输入“默认”按 C/C2 处理。完整 Prompt 默认保存在后台，用户输入“展开Prompt”时再展示。若当前 AI 不能联网，明确说明无法执行，并输出可交给联网 AI 使用的“联网执行所需 Prompt / 分轮搜索指令”；不得假装完成联网寻源。

## 一、快速模式启动口令

```text
启动 AI寻源需求澄清Skill V2.1.0，使用 A 快速寻源。我要寻找：【填写物料、服务或替代对象】。

请自动判断物料类型并选择合适的追问模板，不需要我确认分类。一个问题一个问题追问，优先确认会影响规格、供应商类型、报价和验收的一级信息；我不知道的内容直接记为“待供应商确认”，不要换个说法反复追问。

信息完整度达到约80%后停止追问，在后台生成寻源需求卡和完整 Prompt，然后联网形成10—15家候选方向。默认只展示用户结果版，不展示完整 Prompt；我输入“展开Prompt”时再展示。不得编造企业、参数、资质、联系方式或案例。
```

## 二、深度模式启动口令

```text
启动 AI寻源需求澄清Skill V2.1.0，使用 B 深度寻源。我要寻找：【填写物料、服务或替代对象】。

请自动判断物料类型并选择合适的追问模板，一个问题一个问题追问；未知信息写入“待供应商确认”。信息完整度达到约80%后停止需求追问，在后台生成寻源需求卡并联网进入深度寻源执行。

深度模式请自动生成关键词矩阵和分平台搜索策略，**先做产业带分析判断可能产区（V2.0 修订：产业带只决定搜索优先顺序，不是候选池准入边界，不得以"不在预设产区"排除企业，全国省份未经用户指令不得直接排除）**，再建立30—50家候选池，为每家整理证据链、风险疑点和人工确认问题，按A/B/C/D分层。**品类准入制度判断→全量名录渠道优先（V2.0）：品类有强制许可/认证/备案制度→先跑官方资质库建行业全景名单；无强制资质→企查查国标行业枚举+排污许可证平台兜底；两者皆无（通用加工/贸易品/新兴品类）→尽力枚举并前台如实标注"本品类无全量名录"。关键词矩阵必须包含上游品类词（目标品名之外同时搜其上游品类全集词，如岩棉条→岩棉板/岩棉生产厂家）**。**追问阶段必须主动确认供应商区域范围（限定/排除/全国均可，与收货地区分；未确认按全国搜索并前台标注，不得自行替用户收窄）和已知供应商种子名单（现合作/曾合作供应商，用于召回校验）（V1.9.3）**。**A类每家执行三层定向核验（V1.5）：企业真实性→产品匹配→采购适配**，并按 [[15_漏网检查执行流程]] 真正执行多轮补搜。**漏网检查第6轮必须做种子名单召回校验（V1.9.3/V2.0）：种子供应商未出现在候选池=搜索盲区硬信号，逐家分析漏搜原因（关键词盲区/渠道盲区/词面盲区/企业名差异）并定向补搜；召回失败≥1家即强制切换全量名录渠道重建基准名单，不得继续关键词微调；召回率写入第7轮覆盖性总结**。**候选池数量级覆盖率检查（V2.0）：候选池建完估算行业总厂数并计算覆盖率写入覆盖性总结，覆盖率明显偏低且全量名录可枚举时先补全量名录再定档**。**工商核验优先用企查查/天眼查连接器查询（V1.6），无连接器时降级网页搜公开页并标注"待人工核验"**。**1688数据优先用八爪鱼连接器云采集（V1.7，模板2850；V2.0.2 实测连接器可用，关键词必须用单个短词，严禁空格组合长词——组合词会采回完全不相关商品），无连接器时降级网页搜索**。**寻源前先查 [[17_供应商验证档案库]] 和 17a_供应商档案数据（V1.8/V1.9），命中的引用历史结论不重复调用连接器，已判定贸易商的按供应商类型偏好三档处理（V1.9.2：偏好A排除/偏好B可进A类/偏好C最高B类）；三层验证和电话验证完成后按档案格式生成待审核档案卡，经我确认后写入17a，负例身份事实也生成档案卡，"排除"仅限诚信类/能力类硬负例或用户明确拒绝，git提交/推送须我单独授权，不得自动执行（V2.1.0）**。**第二层参数验证按三态标注（V1.9）：硬性规格每项只标 ✅有证据/❓无证据/❌有反证，❓不降级转电话必问，仅❌反证才降级——AI只判断证据充不充分，不判产品合不合格**。**证据通道按漏斗触发（V1.9）：快通道（企查查+店铺页）每家必跑，中通道（AI网页打开1688商品详情页读SKU参数，已实测不登录可读约60/65规格；八爪鱼2857已实测不可用，不要选用）和反证通道（认监委cx.cnca.cn证书验真）只对快通道晋级候选执行；任一通道失败记❓"通道未采到"继续流程不阻塞**。漏网新增候选必须重新核验证据和分层。默认展示结果版；完整过程和反馈闭环建议放后台。A类只是优先电话验证对象，不是合格或已认证供应商。

输出前执行强制检查：①分层名单每家必带官网链接和公开联系方式（注明来源和获取日期，未找到写"未找到"，严禁编造）；②漏网检查第7轮覆盖性总结作为前台独立小节展示；③结果末尾自动附带电话验证反馈表和回写规则，不等用户询问；④产业带分析已前置（V1.5）；⑤平台专用关键词已按平台维度分别生成（V1.5）；⑥A类三层验证已执行（V1.5）；⑦供应商类型偏好已按三档联动关键词和贸易商分层位置（V1.5/V1.9.2）；⑧工商核验已优先用连接器查询，无连接器时降级网页搜索并标注"待人工核验"（V1.6）；⑨1688采集已优先用八爪鱼云采集并去重，无连接器时降级网页搜索（V1.7）；⑩寻源前已查档案库，命中企业已标注"档案已验证"并引用历史结论，贸易商已按类型偏好三档处理（V1.8/V1.9.2）；⑪三层验证和电话验证完成的企业已按17号档案格式生成待审核档案卡，经用户确认后才写入17a，提交/推送已单独授权且未自动执行（V1.8/V1.9/V2.1.0）；⑫A类第二层硬性规格每项已三态标注（✅/❓/❌），仅❌反证降级，❓无证据已转电话必问清单，⚠️间接风险信号最高降至C类"可快速确认"、未进D类（V1.9/V2.1.0）；⑬证据通道按漏斗分级触发，中通道/反证通道未对全部候选全量跑，通道失败未阻塞流程（V1.9）；⑭供应商类型偏好三档已确认并贯通（需求卡/关键词/分层/档案命中），档案"排除"且排除原因仅为贸易商身份、本次偏好B/C时已提示是否重新纳入（V1.9.2）；⑮负例回写状态已分级，单纯贸易商身份未武断标"排除"（V1.9.2）；⑯供应商区域范围已确认（未确认时按全国搜索并前台标注，未自行替用户收窄），已知供应商种子名单已征集并完成召回校验，召回失败企业已分析原因并定向补搜，召回率已写入覆盖性总结（V1.9.3）；⑰品类准入制度判断已完成，有强制资质库的品类已先跑全量名录建基准名单（V2.0）；⑱关键词矩阵已包含上游品类词，产业带仅作搜索排序、未当准入边界（V2.0）；⑲召回失败≥1家时已强制切换全量名录渠道而非继续关键词微调（V2.0）；⑳候选池行业覆盖率已估算并写入覆盖性总结，覆盖率明显偏低且名录可枚举时已先补全量名录（V2.0）；㉑候选池每家已按"同应用场景供货经验"字段核验，判定标准为该产品在此使用场景有无**明确的生产销售记录**（可查证形式：场景专用产品线在售/该场景客户案例/政府登记/该用途口径检测报告；有记录✅注明来源/无记录❓不降级转电话必问），A 类内部排序以场景生产销售记录为首选依据、优先于规模/品牌/运距，且使用场景已追问到具体产品/工序层级（V2.0.2/V2.0.3）；㉒前台分层输出字段完整性：公司名称全部使用工商可核验全称（仅在B2B软文出现、企查查无主体的虚标企业降C类"大概率不相关"并标注），每家带官网链接+公开联系方式（企查查连接器联系方式查询优先，无连接器降级网页搜），A/B/C/D四层齐全且每层附一句分层依据，C/D为空时也显式写明"无"，不得只列A/B两层（V2.0.4）。
```

## 三、先快速后深度启动口令

```text
启动 AI寻源需求澄清Skill V2.1.0，使用 C 先快速后深度模式。我要寻找：【填写物料、服务或替代对象】。

请先自动判断物料类型，一个问题一个问题追问，信息达到约80%后停止需求追问，在后台生成需求卡并联网形成10—15家初步候选方向。默认只展示结果版。

第一阶段结束后说明覆盖范围和缺口；只有我确认继续、快速结果明显不足或发现明显漏搜风险时，才进入关键词矩阵、分平台搜索策略、30—50家候选池、证据链、A/B/C/D分层和多轮漏网检查。反馈回写只生成模板优化建议，不自动修改主 Skill。未知信息写“待供应商确认”，不得编造企业或证据。本次地域、供应商类型和候选数量等信息只用于本次寻源，不询问是否保存默认偏好。
```
