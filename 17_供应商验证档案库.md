# 17 供应商验证档案库

本模板解决 V1.7 的最大缺陷：**历史验证结论零沉淀**。已用企查查/八爪鱼/电话验证过的供应商结论，若只存在于单次对话，下次寻源会重复验证、白烧连接器，甚至在用户只要生产厂家时把已判定贸易商的企业重新放进 A 类。

档案库让每次寻源"带着记忆跑"：**先查档案 → 再新建验证 → 验证后回写**。

---

## 一、档案库定位

- 沉淀已验证供应商的核验结论（企业真实性、产品匹配、采购适配、供应商类型判定）；
- 跨任务复用：寻源时先查档案，命中则直接引用结论，只补本次需求相关字段；
- 作为反馈回写（14 号模板）的落点：电话验证结论和三层验证结论统一回写到这里。

档案库不等于合格供应商库或认证供应商名单。档案里的"已验证工厂"仍须按本次需求的三层验证执行；档案只提供已核验的事实，不提供自动合格结论。

## 二、档案格式

**数据文件位置（V2.1.0 数据与规则分离）**：档案数据不在本 skill 目录内，实际存放在 `D:\AI寻源skill\17a_供应商档案数据.md`。全库规则文件中提及"17a_供应商档案数据"均指该外部文件；该文件含真实企业信息与核验判断，**不随 skill 安装、提交或推送**。文件不存在时视为空档案库，从零开始积累。

每家已核验企业一条记录，按以下字段追加到 17a_供应商档案数据（V1.9 起档案数据独立存放，本文件只保留规则和格式定义）：

```text
公司名称：
统一信用代码：（企查查连接器返回的准确代码；未查到写"未查到"）
核验结论：真实生产厂家 / 正规贸易商（有实际团队/进出口资质） / 纯贸易商（无制造证据） / 疑似贸易商（冒充需标注） / 身份未核实
核验层级：三层全量验证 / 第一二层 / 第一层 / 多源交叉计数 / 电话验证
证据摘要：（最硬的 1-2 条证据，如"参保0人、经营范围纯批发""股东含XX制造厂""持有XX专利"）
判定关键项：经营范围是否含制造 / 参保人数 / 股东是否含生产厂 / 是否有专利或出口记录（填是/否/未知）
核验方式：企查查连接器 / 天眼查连接器 / 网页搜公开页 / 八爪鱼云采集 / 电话 / 书面资料
核验日期：
来源任务：（如"台阶钻头寻源"）
当前状态：可复用 / 待观察 / 排除
备注：
```

已有首批档案记录（舒城僖浩蕴、杭州钟鼎，台阶钻头寻源实测），存于 17a_供应商档案数据，回写时直接追加在其末尾。

## 三、三段式流程

### 第一步：先查档案（寻源开始时 + 候选池建立前 + 每个新候选加入时）

1. 收到物料需求后，AI 先读取 17a_供应商档案数据，检索是否存在与本次物料相关的已验证企业（按品类、地域、公司名模糊匹配）；
2. 深度模式建立候选池前，把档案命中的企业**优先纳入候选池并标记"档案已验证"**；
3. 用户提供的已知供应商（现有供应商/验证过/排除的），先查档案，命中的直接引用历史结论并展示给用户确认，不再重复验证；
4. 档案命中但结论已过时或与本次需求冲突时，按第六节"档案更新规则"处理。

### 第二步：再新建验证（档案未命中时才新建）

1. 档案未命中的候选，按 [[13_候选供应商证据链与分层模板]] 正常执行三层验证；
2. 三层验证过程中发现某家企业已在档案中，直接引用档案结论，不重复调用连接器；
3. 档案里"待观察"或"排除"状态的企业再次出现时，必须向前台标注历史状态，不得静默覆盖。

### 第三步：验证后回写（生成待审核档案卡 → 用户确认写入 → 单独授权提交推送）

1. A 类三层验证完成的企业，按第二节档案格式**生成待审核档案卡**（V2.1.0：默认不直接写入）；
2. 电话验证反馈（[[14_人工验证与反馈回写模板]]）中"是否加入供应商库=是"的企业，按档案格式生成档案卡；**负例按 14 号规则 11 执行（V1.9.2）**：身份事实必生成档案卡，"排除"状态仅限诚信类/能力类硬负例或用户明确拒绝，单纯贸易商身份按本次类型偏好决定状态；
3. 生成档案卡保持轻量：每家 2 分钟内可完成，只写能影响后续判断的字段；
4. **写入授权（V2.1.0）**：AI 默认只生成待审核档案卡并在前台展示（或存为草稿文件）；**用户明确说"写入档案"（或等效确认）后**，才追加到 17a_供应商档案数据。同一轮确认只覆盖写入，不包含提交和推送；
5. **提交推送独立授权（V2.1.0，废止 V1.9.1 自动同步）**：写入完成后，**用户明确说"提交"/"推送"（或等效指令）** 才执行 git 提交/推送（WorkBuddy 侧仓库）和 Codex 副本同步（`.codex/skills/ai-sourcing-requirement-clarification/`）；未授权时前台提示"档案已写入本地，尚未提交推送"，由用户决定时机。**禁止为完成双副本同步而自动执行任何提交、推送或外部仓库操作**——供应商负面判断、联系方式等档案内容一旦推送即永久进入远程仓库，必须由用户知情决定。

## 四、档案命中后的处理规则

| 档案状态 | 处理方式 |
|---|---|
| 已验证为"真实生产厂家" | 标记"档案已验证-工厂"，仍按本次需求做第二层（产品匹配）和第三层（采购适配）验证，第一层引用档案结论不再重复查 |
| 已验证为"纯贸易商/疑似贸易商" | 标记"档案已验证-贸易商"，按需求卡供应商类型偏好分档处理（V1.9.2）：偏好 A（只接受生产厂家）→ 不纳入候选池；偏好 B（无优先级）→ 与工厂同标准竞争，可进 A 类；偏好 C（工厂优先）→ 最高 B 类，工厂候选排完再排贸易商 |
| 已验证为"正规贸易商" | 标记"档案已验证-进出口贸易商"，同样按偏好三档处理（V1.9.2，规则同上）；偏好 B/C 时提示用户"反查上游"价值 |
| 档案"待观察" | 正常走当前寻源验证流程，但前台标注"历史档案：待观察" |
| 档案"排除" | 默认不纳入候选池；**排除原因仅为贸易商身份**的企业，若本次偏好为 B 或 C，前台主动提示"档案有此企业（贸易商），是否重新纳入"；用户明确要求重新评估时，需先用连接器重新核验再定去留 |

## 五、与现有流程的衔接

- **反馈闭环落点**：[[14_人工验证与反馈回写模板]] 的"是否加入供应商库=是"回写到 17a_供应商档案数据；
- **证据链字段**：[[13_候选供应商证据链与分层模板]] 证据链字段表新增"档案库命中状态"（V1.8）：命中档案结论 / 未命中新建验证；
- **前台展示**：[[16_前后台输出分层规则]] 前台名单中，命中档案的企业标注"档案已验证"，并展示历史结论摘要；
- **漏网检查**：[[15_漏网检查执行流程]] 每轮新增候选同样执行"先查档案"；
- **后台内容**：档案查询过程和回写记录默认放后台，可按"展开档案记录"口令查看。

## 六、档案更新规则

1. **证据优先级分领域（V2.1.0，取代"电话结论一律最高优先"）**：电话/连接器/网页结论冲突时按事实类型裁决——**经营事实**（MOQ、模具费、接单意愿、当前产品线、产能排期）以电话为准，因工商登记不含且时效最新；**法律事实**（注册资本、股东、经营状态、成立日期）以工商登记/官方登记为准，供应商口头说法不得覆盖；无法裁决的标"证据冲突"待人工确认，不直接覆盖任一方。回写冲突结论时两说并记，标注来源和日期；
2. 结论变化（如贸易商确认背后工厂、工厂停业）必须记录变更日期和新证据，不直接覆盖历史记录；
3. 17a_供应商档案数据 保留历史记录，不删除；新的核验追加为新条目并标注"更新"；
4. 同一公司用统一信用代码锁定，避免同名企业混淆；名称变化时备注"曾用名"；
5. 档案"可复用"状态默认保留，长期（如超 1 年）未复用的企业重新使用前建议先刷新工商状态。
