# 16 前后台输出分层规则

## 一、核心原则

默认让用户看到结果，不让普通用户看到完整提示词和后台过程。后台过程用于复盘、调试和优化 Skill，不默认展示。

## 二、前台默认展示

- 本次需求摘要；
- 推荐供应商名单；
- A/B/C/D 分层结果；
- 每家推荐理由；
- 每家官网链接和公开联系方式（必展示字段，注明来源和获取日期，未找到写"未找到"，严禁编造，不得整列省略）；
- 每家工商核验状态、1688店铺类型、多源交叉计数（V1.5 必展示字段；工商核验优先用企查查/天眼查连接器查询，无连接器时网页搜公开页并标注"待人工核验"——V1.6）；
- 每家风险/待确认问题；
- 优先联系顺序；
- 电话沟通话术；
- 微信询价话术；
- 下一步操作建议；
- 深度模式的产业带分析简述（V1.5）；
- 深度模式的漏网检查覆盖性结论（独立小节）；
- 深度寻源结果的电话验证反馈表（自动附带，不等用户询问）；
- 档案命中的企业标注"档案已验证"并展示历史结论摘要（V1.8，如"档案判定：贸易商""档案判定：工厂"）。

A类只表示优先电话验证对象，不是合格供应商或已认证供应商。

## 三、后台默认隐藏

- 完整需求卡；
- 关键词矩阵；
- 搜索策略；
- 完整 Prompt；
- 产业带分析完整过程（V1.5）；
- 每轮搜索记录；
- 候选池原始名单；
- 每家完整证据链；
- 三层验证逐层记录（V1.5）；
- 漏网检查每轮补搜过程；
- A/B/C/D 分层详细依据；
- 人工验证回写表；
- 模板优化建议；
- 档案查询与回写记录（V1.8：查了哪些历史档案、命中哪些、回写了哪些）。

## 四、展开口令

用户可通过以下口令查看对应后台内容：

- 展开需求卡；
- 展开关键词矩阵；
- 展开搜索策略；
- 展开Prompt / 展开完整Prompt；
- 展开证据链；
- 展开漏网检查记录；
- 展开反馈回写表；
- 展开档案记录（V1.8）；
- 展开后台过程。

“展开后台过程”表示展示全部可用后台内容；其他口令只展示对应模块。

## 五、Prompt 展示与联网降级

- A/A2、B/B2、C/C2 联网执行时，完整 Prompt 默认放后台，不在普通用户结果页展示；
- 用户明确输入“展开Prompt”或“展开完整Prompt”时再展示；
- 当前环境无法联网时，可以输出“联网执行所需 Prompt / 分轮搜索指令”；
- 降级输出必须明确说明尚未完成联网寻源，不得生成或暗示已核验的供应商结果；
- Prompt 是执行指令，不是正式用户模式选项。

## 六、模式与后台映射

- 用户输入 A 或 A2：执行快速寻源；
- 用户输入 B 或 B2：执行深度寻源；
- 用户输入 C、C2 或“默认”：执行先快速后深度；
- A1/B1/C1 不再作为正式用户选项；
- 本次到货地、地域偏好、供应商类型和候选数量只用于本次寻源，不保存为默认偏好，也不询问是否保存。

## 七、反馈隔离

反馈回写和模板优化建议默认放后台。反馈只形成建议，不自动修改主 Skill；只有用户或维护者明确确认后，才允许实施修改。
